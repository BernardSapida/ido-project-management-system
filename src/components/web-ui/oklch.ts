/**
 * `@bernardsapida/web-ui/oklch` - the colour maths on its own.
 *
 * A separate entry because the app's theme system (palette.build, presets,
 * designer-palette, pairings) needs the arithmetic without pulling in a single
 * React component.
 */
export * from "./lib/oklch";
